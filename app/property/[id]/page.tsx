import {notFound} from 'next/navigation'; import {properties} from '@/lib/data'; import Link from 'next/link';
export default async function PropertyPage({params}:{params:Promise<{id:string}>}){const {id}=await params; const p=properties.find(x=>x.id===id); if(!p)return notFound(); return <main className="detailhero"><div className="container"><Link href="/search" className="muted">← Back to properties</Link><div style={{marginTop:16}} className="gallery"><img src={p.image} alt={p.title}/><div><img className="small" src={p.image} alt="Property"/><img className="small" src={p.image} alt="Property" style={{marginTop:10}}/></div></div><div className="detailgrid"><div className="panel"><h1 style={{marginTop:0}}>{p.title}</h1><div className="muted">📍 {p.location}</div><div className="price">{p.price}</div><div className="meta"><span className="pill">{p.area}</span><span className="pill">{p.type}</span>{p.verified&&<span className="pill">✓ Verified</span>}</div><p style={{lineHeight:1.7}}>{p.description}</p><h3>Property details</h3><div className="featurelist"><div className="feature"><b>Area</b><br/>{p.area}</div><div className="feature"><b>Road access</b><br/>{p.road}</div><div className="feature"><b>Location</b><br/>{p.city}, {p.state}</div><div className="feature"><b>Listing</b><br/>{p.verified?'Verified':'Standard'}</div></div><h3>Location</h3><div style={{height:220,borderRadius:16,background:'#e4ebe6',display:'grid',placeItems:'center'}}>📍 Map coming in V2 · {p.location}</div></div><aside className="panel"><h3 style={{marginTop:0}}>Interested in this property?</h3><p className="muted">Send the seller your details and arrange a conversation.</p><input className="input" placeholder="Your name"/><br/><br/><input className="input" placeholder="Mobile number"/><br/><br/><textarea className="input" rows={4} placeholder="I am interested in this property..."/><br/><br/><button className="btn primary" style={{width:'100%'}}>Send Enquiry</button></aside></div></div></main>}
